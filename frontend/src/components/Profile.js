import React from "react";

const Profile = () => {
  return (
    <div>
      Since this is not an authentication tutorial, you will use React useState
      hook to simulate a login system.
    </div>
  );
};

export default Profile;
